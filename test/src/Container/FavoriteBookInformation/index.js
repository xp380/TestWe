import { connect } from "react-redux";
import FavoriteBookInformation from "../../Components/FavoriteBookInformation/index";

const mapStateToProps = (state) => ({});

export default connect(mapStateToProps)(FavoriteBookInformation);
